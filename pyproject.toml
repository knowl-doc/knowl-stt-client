[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "knowlsttclient"
version = "0.1.0"
description = "Python client for Triton inference server"
dependencies = ["protobuf", "requests", "websockets"]
